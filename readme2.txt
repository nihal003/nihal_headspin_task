Imported the module fractions for using built in function gcd() used to find greatest common divisor (gcd).
Initialised variable "x" as "i" is used to calculate the formula "x=(x*i)/fractions.gcd(x,i)" in a loop from 1 to 20.
After the loop "x" is printed for the output